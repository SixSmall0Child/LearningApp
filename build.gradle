// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    repositories {
        google()
        jcenter()

        maven {
            url 'https://maven.aliyun.com/repository/public'
        }
        maven {
            //仓库账户密码，只能用于拉取aar库，不能用于登录管理后台
            credentials {
                username '60d14a0a3c458713dd1670a3'
                password 'Dns95c1WdP2K'
            }
            url 'https://packages.aliyun.com/maven/repository/2113755-release-JVQtCP/'
        }
        maven {
            //仓库账户密码，只能用于拉取aar库，不能用于登录管理后台
            credentials {
                username '60d14a0a3c458713dd1670a3'
                password 'Dns95c1WdP2K'
            }
            url 'https://packages.aliyun.com/maven/repository/2113755-snapshot-nr4t6k/'
        }


    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.1.3"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}